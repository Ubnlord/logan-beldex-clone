# Components

Place reusable UI components here (Header, BottomNav, DepositForm, WithdrawalForm,
TransactionList, ReferralCard, etc.).

The original uploaded artifact contains a bundled production React application.
The generated `src/original-bundle.js` is preserved separately so no runtime
behavior is silently changed during the structural migration.
