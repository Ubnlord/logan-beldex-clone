# Logan-Beldex Organized V2

Source of truth: `logan-beldex-organized-v2.html`.

This project begins from the exact original HTML artifact supplied by the user. The original production React bundle and CSS are preserved before any component-level refactor.

## Next refactor stages
1. Extract authentication screens.
2. Extract dashboard and navigation.
3. Extract deposit/withdraw/history/referral screens.
4. Move reusable UI into `src/components`.
5. Move data/constants into `src/data`.
6. Add tests and verify build before deployment.
